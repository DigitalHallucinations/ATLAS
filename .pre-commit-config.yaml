repos:
  - repo: local
    hooks:
      - id: atlas-change-validation
        name: atlas-change-validation
        entry: python scripts/validate_changes.py
        language: system
        pass_filenames: true
        stages:
          - commit
          - push
